# image-classification
Image classification project implementing machine learning models (Random Forest &amp; SVM) to classify handwritten digits from the MNIST dataset. Built with scikit-learn, featuring data preprocessing, dimensionality reduction (PCA), model evaluation, and inference pipeline.
